#pragma once

class Mesh {
	
private:
	void* vertexData;
	size_t vertexDataSize;

	void* indexData;
	size_t indexDataSize;
};

class Material {
private:
	// Параметры материала, например, цвет, текстуры и т.д.

};